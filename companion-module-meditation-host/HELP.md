# 靜心主持台 Companion 模組

1. 先啟動 macOS 的「靜心主持台」App。
2. 在 Companion 安裝此模組，新增連線時使用 `127.0.0.1`、連接埠 `4747`。
3. 從 Presets 拖入開始、暫停、重設、結束、各種分鐘與動態倒數按鈕。

「自訂分鐘」preset 預設為 12 分鐘；放到按鈕後，在 Action 的「分鐘」欄輸入任何 1–999 的數值。

動態倒數按鈕使用模組變數 `$(this:remaining)`，每 250 毫秒從 App 更新一次。
