# 靜心主持台

## 連線

1. 先啟動 macOS 的「靜心主持台」App。
2. 新增「靜心主持台」連線。
3. 同一台 Mac 使用位址 `127.0.0.1`、連接埠 `4747`。

## Presets

模組提供開始、暫停／繼續、重新準備、提早結束、3／5／7／10／15／20 分鐘、自訂分鐘及動態倒數按鈕。

「自訂分鐘」預設為 12 分鐘。將 preset 放到按鈕後，可在 Action 的「分鐘」欄改成任何 1–999 分鐘。

動態倒數按鈕使用 `$(Meditation:remaining)`，約每 250 毫秒更新。
