# ZenTime 專案結構

## 目前使用中的原始碼

- `cross-platform/`：macOS／Windows Electron 桌面版，最新版建置產物在 `cross-platform/dist/`。
- `Sources/`、`Resources/`、`Assets/`：macOS 原生版原始碼與資源。
- `docker-version/`：Docker／NAS 網頁版。
- `companion-module-meditation-host/`：Bitfocus Companion 模組；目前套件為 `sunda-meditation-host-0.6.1.tgz`。
- `ZenTime-site/`：NAS 官網來源與下載檔。
- `docs/`：GitHub Pages 官網。
- `scripts/`、`build-app.sh`：建置工具。

## 封存資料

`封存資料/` 只保存舊發行檔，未刪除任何內容：

- `跨平台歷史版本/`：1.0.x 桌面版套件。
- `macOS原生歷史版本/`：0.9.x DMG。
- `Companion歷史版本/`：0.1.0～0.6.0 與早期中文命名套件。
- `Docker歷史套件/`：早期 QNAP Docker 壓縮檔。

## 建置與下載檔

- 最新桌面版：`cross-platform/dist/`。
- 官網下載副本：`ZenTime-site/downloads/`。
- `.build/`、`node_modules/` 與 `dist/` 是建置或相依套件目錄，為避免影響開發環境，本次整理全部保留。
